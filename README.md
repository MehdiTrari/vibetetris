# vibetetris

Un Tetris front-only en `HTML/CSS/JS`, avec une direction visuelle pixel art médiévale.

## Lancer le jeu

Ouvre `index.html` dans un navigateur, ou sers le dossier avec un serveur statique:

```bash
python3 -m http.server 8000
```

Puis visite `http://localhost:8000`.
